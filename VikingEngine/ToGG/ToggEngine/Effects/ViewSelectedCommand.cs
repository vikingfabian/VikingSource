using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using VikingEngine.ToGG.Commander.CommandCard;

namespace VikingEngine.ToGG.Effects
{
    class ViewSelectedCommand
    {
        public ViewSelectedCommand(CommandType commandType)
        {
            //var card = CommandCard.AbsCommandCard.Card(commandType, null);
            //card.ParentPosition = Engine.Screen.CenterScreen - HudLib.cardSize * 0.5f;
            //new Timer.ActionEventTimedTrigger(card.DeleteMe, new Time(1, TimeUnit.Seconds));
        }
    }
}
