using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace VikingEngine.EngineSpace.Extensions.List
{
    /*
    class SortedTreeCounter<T> where T : IComparable
    {

    }

    /// <summary>
    /// A binary tree with sorted IComparable object
    /// </summary>
    class SortedTree<T> where T: IComparable
    {
        SortedTreeNode<T> rootNode = null;

        public SortedTree()
        {
            //rootNode = new SortedTreeNode<T>();
        }

        public void Add(T value)
        {

            ++Count;
        }

        public int Count { get; private set; }
    }
    class SortedTreeNode<T> where T : IComparable
    {
        T value;
        SortedTreeNode<T> lowerNode, higherNode, parentNode;

        public SortedTreeNode(T value)
        {
            this.value = value;
        }

        public void Add(T value)
        {
            if (value)
        }
    }
     */
}
